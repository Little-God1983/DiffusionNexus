name: .NET 10 Build & Test

on:
  push:
    branches: [develop]
  pull_request:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      packages: read
      contents: read

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET 10
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x'

    - name: Restore dependencies
      run: dotnet restore
      env:
        GITHUB_PACKAGES_TOKEN: ${{ secrets.PACKAGES_PAT }}

    - name: Restore for third-party notices check (RID-specific, self-contained)
      run: dotnet restore DiffusionNexus.UI -r win-x64 -p:SelfContained=true

    - name: Check third-party notices are current
      shell: pwsh
      run: pwsh Scripts/Generate-ThirdPartyNotices.ps1 -Check

    - name: Build
      run: dotnet build --no-restore --configuration Release

    - name: Install EF Core tools
      run: dotnet tool install --global dotnet-ef

    - name: Check for pending EF model changes
      run: dotnet ef migrations has-pending-model-changes --project DiffusionNexus.DataAccess --startup-project DiffusionNexus.DataAccess --context DiffusionNexusCoreDbContext --no-build --configuration Release

    - name: Test
      run: dotnet test --no-build --configuration Release --verbosity normal
