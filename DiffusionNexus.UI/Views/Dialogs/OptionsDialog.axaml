<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="using:DiffusionNexus.UI.Views.Dialogs"
        x:Class="DiffusionNexus.UI.Views.Dialogs.OptionsDialog"
        x:DataType="local:OptionsDialog"
        MinWidth="480"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        Background="#1E1E1E">
  
  <Grid Margin="24" RowDefinitions="Auto,Auto">
    
    <!-- Message. MaxWidth makes it wrap during measure (the window sizes to content), so the
         window grows for the buttons but never past a readable line length. -->
    <TextBlock Grid.Row="0" 
               Text="{Binding Message}"
               TextWrapping="Wrap"
               MaxWidth="600"
               HorizontalAlignment="Left"
               FontSize="14"
               Margin="0,0,0,24"/>
    
    <!-- Buttons -->
    <ItemsControl Grid.Row="1" 
                  ItemsSource="{Binding OptionItems}"
                  x:Name="OptionsPanel">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel Orientation="Horizontal" 
                      HorizontalAlignment="Right" 
                      Spacing="8"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Button Content="{Binding Label}"
                  MinWidth="120"
                  Padding="16,8"
                  Background="{Binding Background}"
                  Click="OnOptionClick"
                  Tag="{Binding Index}"/>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    
  </Grid>
</Window>
